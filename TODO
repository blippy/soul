-*- org -*-

* purge acfg from system, as it seems redundant these days
* use the cheetah templating system

* Refactor
n = datetime.date.today()
fname = www() + 'archives/' + str(n) + '.htm'
file(fname,"w").write(html)
